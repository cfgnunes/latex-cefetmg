language: generic

dist: xenial
addons:
 apt:
   sources:
     - ubuntu-toolchain-r-test
   packages:
     - make
     - latexmk
     - texlive
     - texlive-lang-portuguese
     - texlive-latex-extra
     - texlive-publishers
     - texlive-science
     - texlive-xetex
     - ghostscript

script:
  - make clean
  - make compile-test
  - make compress
